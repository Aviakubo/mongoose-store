<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/uikit-rtl.css"/>
    <link rel="stylesheet" href="/uikit-rtl.min.css"/>
    <link rel="stylesheet" href="/uikit.css"/>
    <link rel="stylesheet" href="/uikit.min.css"/>
    <title>Mongoose Store</title>
</head>
<body>
    <nav>
        <a href="/store">Back to Main Store</a>
        <a href="/store/<%=store._id%>/edit">Edit Product</a>
    </nav>
    <h1><%=store.name %></h1>
    <img id="pic" src="<%= store.img%>" alt="store.name"><br>
    Description: <%= store.description%>
    <br />
    Price: <%= store.price %>
    <br>
    Quantity Remaining: <%= store.qty%>
    <!-- BUY -->
    <form action="/store/<%=store._id%>?_method=PUT" method="POST">
    <% if(store.qty === 0) { %>
        <p>OUT OF STOCK</p>
        <input type="submit" value="Buy" disabled=true>
        <% } else { %>    
            <input type="hidden" name="qty" value="<%= (parseInt(store.qty)-1).toString() %>" >
            <input id="buy" type="submit" value="Buy">
    <% } %>
</form>
<!-- DELETE -->
<form action="/store/<%=store._id%>?_method=DELETE" method="POST">
    <input type="submit" value="Delete Product">
</form>
    
<% /* %> 
<% let qty = store.qty %>
<% let buy = document.getElementById("buy")%>
<% %> 
<% */ %>
</body>
</html>